/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "hips.hearstapps.com",
      "laxmiflourmill.com",
      "chetanagro.com",
    ],
  },
};

export default nextConfig;
